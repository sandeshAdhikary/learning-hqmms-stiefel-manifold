%% Connect MATLAB to Python

% CHANGE python_path AS NEEDED!
% python_path = '/home/siddarthsrinivasan/anaconda3/envs/py3.5/bin/python';
% pyversion(python_path)

% insert(py.sys.path,int32(0),'');

% Add utils (current folder) to path
% if count(py.sys.path,pwd) == 0
%     insert(py.sys.path,int32(0),pwd);
% end